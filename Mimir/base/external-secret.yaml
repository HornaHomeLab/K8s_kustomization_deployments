apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: mimir-minio-creds
spec:
  refreshInterval: "1h"
  secretStoreRef:
    name: hcp-vault-minio-accounts
    kind: ClusterSecretStore
  target:
    name: mimir-minio-creds
  data:
  - remoteRef:
      key: "mimir-user"
      property: "secret_key"
    secretKey: "secret_key"
  - remoteRef:
      key: "mimir-user"
      property: "access_key"
    secretKey: "access_key"