apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: metrics
  labels:
    release: prometheus
spec:
  selector:
    matchLabels:
      app: mimir
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
