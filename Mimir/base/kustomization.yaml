apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: mimir

resources:
  - namespace.yaml
  - pvc.yaml
  - external-secret.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - service-monitor.yaml
  

configMapGenerator:
- name: mimir-config
  files:
  - config.yaml=conf/mimir-config.yaml
  options:
    disableNameSuffixHash: true
