apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: loki-ingress-route
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`REPLACE_ME_HOST`)
      kind: Rule
      services:
        - name: loki-service
          port: 3100