apiVersion: v1
kind: Service
metadata:
  name: service
  labels:
    app: loki
spec:
  selector:
    app: loki
  ports:
    - name: http
      protocol: TCP
      port: 3100
      targetPort: 3100
  type: LoadBalancer